# 开发日志（Dev Log）

记录每一轮代码开发的过程性发现：审查抓到的问题、为什么重要、设计推理中
值得留存的部分。与 `tuning_log.md`（调参）、`experiment_log.md`（实验运行）
平行；设计结论的正式归宿是 `project_flow.md` 和对应 spec，这里只记过程和
"为什么"，不重复设计本身。

---

## 2026-07-09 · RQ2 模型层（第一轮实现）

- 范围：`npc_policy/features.py`（phi 特征 + case_inputs）、`npc_policy/learned.py`
  （SimplePolicy / NonlinearPolicy / AgnosticPolicy / UniformBaseline / kl_loss /
  predict_distribution）、`tests/test_learned.py`（28 个新测试）。
- 设计存档（英文）：`docs/specs/2026-07-09-rq2-model-layer-design.md`（实现设计）、
  `docs/specs/2026-07-09-rq2-models-and-2a-experiments.md`（研究设计）。
- 施工记录：`docs/plans/2026-07-09-rq2-model-layer.md`（含"执行偏差"附录）。
- 流程：brainstorming → spec → 计划 → 子代理逐组实现，每组两道审查
  （spec 合规 + 代码质量），最后整体终审。终态：全套 49 测试通过、0 警告。

### 设计推理里最值钱的一条：简单模型的可表示性边界

把 ideal-point 老师展开：`−w_f(o_f − μ_f(p))² = −w_f o_f² + 2w_f b_f o_f +
2w_f (Cp)_f o_f − const`。所以给简单模型加 `o²` 特征后，它**原则上能精确表示**：
未裁剪的 ideal-point 基础分、γ·fam 记忆项、ρ·rep satiation 项。它**表示不了**：
μ 的 [0,1] 裁剪、(Cp)² 人格二次项、N 温度（人格对全部打分项的乘性缩放 =
三阶交互，超出二阶双线性特征的表达力）。这三项就是 2A 里简单 vs 非线性
差距的预测来源——差距不再是笼统的"容量不足"，而是可以逐项归因的。
预注册假设（写在 pf §6b.1）：E1 诊断里简单模型跟不上 N 温度通道，
非线性模型（靠 e_p⊙e_o 乘性交互 + 多层非线性）跟得上。

### 审查抓到的三个问题（都已修复）

1. **输入校验不对称**（质量审查，Group A）。`case_inputs` 只拒绝"location 案例
   多带选中地点"，不拒绝"action 案例少带"。后者会静默填零向量——将来数据
   生成脚本若漏传地点，得到的是一批悄悄缺信息的脏数据而非报错。已补对称
   守卫 + 测试。教训：两个方向的约定要两个方向都校验。
2. **计划文本被后续修复搞过时**（实现者上报，Group C/D）。问题 1 的新守卫
   与施工计划里两段测试代码冲突（写计划时守卫还不存在）。实现者按规矩
   停下上报而未擅改，协调者修计划后继续。流程按设计运转的例子。
3. **一个"证明"其实什么都没证明**（整体终审，本轮最有价值的发现）。
   "简单模型精确拟合双线性老师"的测试通过是假象：
   - 40 个案例（约 140 个约束）对 228 个参数——欠定，任何老师都能被插值
     拟合。审查者实测：换上理论上不可表示的 ideal-point 老师，测试照样过。
   - 测试用的老师还开着 `lambda_N=1.5` 的 N 温度，而 N 温度恰在
     "不可表示"清单上——靶子本身就不在可表示范围内。
   - 修法：`lambda_N=0`（两层）+ 每类 200 案例（超定，插值混不过去）。
     修后 KL = 2.5e-9，才是真证明。
   - 为什么重要：2A 要把简单/非线性差距归因到 clip/(Cp)²/N 温度上，这个
     测试是那条论证链的实验地基；没修的话论文引用的是假证据。
   - 教训：**验证"可表示性"的测试必须同时满足（a）靶子确实在可表示类内、
     （b）约束数远大于参数数**，否则通过没有含义。这条对以后 2A 的
     泛化 split 设计同样适用——测试集必须真的在训练分布外，否则"泛化好"
     同样是假象。

### 留给下一轮（2A 数据生成 + 训练循环）的备忘

- 训练循环落地后，给 `predict_distribution` 补一个"调用后正确恢复训练标志"
  的小测试（终审建议；恢复逻辑现在是对的，但要等训练/评估交错调用时
  才真正吃重）。
- `learned.py` 已 226 行，训练循环模块开始依赖它时考虑拆 batch/models
  （终审的非紧急观察）。

---

## 2026-07-10 · RQ2 2A 流水线（第二轮实现）

- 范围：`experiments/rq2/`（common / gen_controlled / train / run_2a /
  run_e_diag）、`tests/test_rq2_pipeline.py`（40 个新测试，全套 86）、
  `docs/rq2_runbook.md`（中文操作手册）。
- 设计存档：`docs/specs/2026-07-10-rq2-2a-pipeline-design.md`；施工记录：
  `docs/plans/2026-07-10-rq2-2a-pipeline.md`（含执行偏差附录）。
- 流程变化：前两组沿用"子代理 + 双重审查"，其后因 token 成本改为协调者
  按计划直接实现 + 一次整体终审。工作方式变化：实验由用户按手册自己跑，
  Claude 只写代码和事后分析（不再托管长任务）。

### 本轮最值钱的发现：G5 在模型输入空间里是空的（质量审查，Critical）

recency 权重按缓冲长度归一化（`relations.py`），所以单一家族缓冲**不管多长**
给模型的 `(rep, sim, nov)` 都一样：`[x]` 和 `[x,x,x]` 都是 rep=1.0。原定
"训练集只排除三连"的 G5 过滤等于什么都没排除——训练集里 12.6% 的案例
已经带着 rep=1.0 输入，"没见过的历史模式"这一泛化主张会被平凡满足。
修法：过滤改为"排除任何长度的单一家族非空缓冲"（训练 rep 上限 ≈0.82，
测试的 rep=1.0 成为真正的外推区），且定向测试案例强制"被重复的选项在
候选集里"（否则天花板根本不出现在测试输入里）。
教训：**泛化划分必须定义在模型可见的输入空间上，不能定义在生成参数上**
——和上一轮"可表示性证明"是同一条原理（split 的"排除"要经得起从输入
反推的对抗检验）。测试也随之加了"反向断言"：用独立复述的排除条件对
每个划分的 train/val 逐条断言 `not check(case)`，防 TRAIN_FILTERS 漏写
`not` 时静默漏题。

### 终审抓到的方法论问题：种子平均分布再算熵会被 Jensen 偏置

E1 的 N 温度检验原来先把 5 个种子的分布平均、再算熵。由 Jensen 不等式
entropy(mean P) ≥ mean entropy(P)，种子间分歧越大熵越被抬高——而分歧
最大的区域恰是预注册检验关心的"简单模型跟不上 N 通道"区域。修为逐种子
算熵/Mantel 再报均值 ± 标准差。教训：**聚合顺序是方法论决定，先平均
后算指标 ≠ 先算指标后平均**，凡是非线性指标（熵、KL、相关）都要先算后聚。

### 其他有价值的修复

- 教师配置漂移守卫：`train` 和 `run_e_diag` 启动时比对当前 `config_hash()`
  与数据集 `meta.json` 里的哈希，不一致直接拒跑——"冻结的教师"从口头
  承诺变成机器检查。
- 断点续跑的原子性补全：结果 JSON / 权重文件也走 tmp+rename（数据池在
  Group A 审查时已改）；中断只会导致重训一个运行，不会留下"看似完成"
  的截断文件。
- 实现者自己抓到计划错误：原生成量下 G6 过滤后不足 10.5 万，全量会中途
  报错；按设计预留的补救（加大 arena 封锁世界轨迹）修正——`generate()`
  里的 RuntimeError 守卫证明了"快速失败"设计的价值。

### 留给下一轮的备忘

- 用户跑完全量后：分析 `results/rq2/`，重点看预注册的 E1 N 熵曲线
  （`e_diag/e1_n_entropy.csv` 已带 std 列）和 G 划分上 filtered-trained vs
  S0-trained 的差值（隔离排除效应）。
- 跨划分测试集成分不同（S0 混合、G2/G3 纯合成 location、G4 纯 rollout），
  写论文时对比要用"同一测试集内"的差值，不要直接横比裸 KL
  （`meta.json` 的 composition 字段记录了各文件成分）。
- RQ1 的消融词表是 `none/location_only/full`，RQ2 改叫 `no_context`；
  论文里两处都出现时需要一句映射说明。

## 2026-07-11 — Study 2B 管线实现

- 按 `docs/specs/2026-07-11-rq2-2b-pipeline-design.md` /
  `docs/plans/2026-07-11-rq2-2b-pipeline.md` 完成 2B 全链路：生成说明书
  （`docs/rq2b_generation_guide.md`，聊天窗口手工路线，无 API 依赖）、
  `import_independent`（校验/配数/切分/覆盖率报告）、`train_2b`（40 run）、
  `run_2b`（含 scorer 与 uniform 的对比主表，即 Study 3 自动化对比）。
- 关键复用：one-hot 目标下 `kl_loss` 数学上等于交叉熵，2A 训练循环零改动
  复用（`train_one` 仅加 `to_inputs` / `weight_decay` 两个注入参数）。
- 隔离由导入脚本强制：G1 区人格（O>0.5∧C<−0.5）与 arena 家族只进结构化
  测试组，超额者丢弃留档（`structured_surplus`），不依赖 AI 遵守说明书。
- 测试：`tests/test_rq2b_pipeline.py` 39 项（校验规则逐条、数值与
  `npc_policy` 参考实现对拍、切分隔离性、端到端冒烟），2A 套件回归通过。
